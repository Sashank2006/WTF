/* ============================================================
   config.js — front-end config injected at build/runtime.
   The API base URL points to the Render backend.
   Update WTF_API_BASE after deploying the server.
   ============================================================ */

window.WTF_CONFIG = {
  API_BASE: 'https://wtf-api.onrender.com',
};